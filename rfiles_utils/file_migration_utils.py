def copy_files_to_dir(to_dirs = [], files_to_copy = []):
    file_and_dir = [[dir, file] for dir, file in zip(to_dirs, files_to_copy)]
'''
    for subpath in subpaths_list[1:]:
        print(subpath)
        copy_files_to_dir(subpath, files_in_dir)
'''